{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "items": [
    {
      "name": "empty-state",
      "type": "registry:component",
      "title": "EmptyState",
      "description": "Standard empty state for 'no data' and 'no results' cases. Default and inline variants. Slots for icon, title, description, primary + secondary action.",
      "files": [
        {
          "path": "empty-state.tsx",
          "type": "registry:component",
          "target": "~/components/empty-state.tsx"
        }
      ]
    },
    {
      "name": "confirm-dialog",
      "type": "registry:component",
      "title": "ConfirmDialog (imperative)",
      "description": "Imperative `await confirm({ title, description })` API backed by AlertDialog. Drop the provider at the app root, then call useConfirm() from anywhere — no JSX boilerplate.",
      "registryDependencies": ["alert-dialog"],
      "files": [
        {
          "path": "confirm-dialog.tsx",
          "type": "registry:component",
          "target": "~/components/confirm-dialog.tsx"
        }
      ]
    },
    {
      "name": "loading-button",
      "type": "registry:component",
      "title": "LoadingButton",
      "description": "Drop-in replacement for the shadcn Button with a built-in spinner + disabled state when `loading` is true. Saves the inline conditional on every async action.",
      "dependencies": ["lucide-react"],
      "registryDependencies": ["button"],
      "files": [
        {
          "path": "loading-button.tsx",
          "type": "registry:component",
          "target": "~/components/loading-button.tsx"
        }
      ]
    },
    {
      "name": "copy-button",
      "type": "registry:component",
      "title": "CopyButton",
      "description": "Click-to-copy button with a 'copied' success state and a Clipboard API fallback for older browsers.",
      "dependencies": ["lucide-react"],
      "registryDependencies": ["button"],
      "files": [
        {
          "path": "copy-button.tsx",
          "type": "registry:component",
          "target": "~/components/copy-button.tsx"
        }
      ]
    },
    {
      "name": "error-boundary",
      "type": "registry:component",
      "title": "ErrorBoundary + DefaultFallback",
      "description": "React error boundary with a styled default fallback (matches design tokens), a retry callback, optional resetKey to clear errors on route/data change, and an onError hook for monitoring (Sentry / captureException).",
      "dependencies": ["lucide-react"],
      "registryDependencies": ["button"],
      "files": [
        {
          "path": "error-boundary.tsx",
          "type": "registry:component",
          "target": "~/components/error-boundary.tsx"
        }
      ]
    },
    {
      "name": "skeletons",
      "type": "registry:component",
      "title": "Layout-matched skeletons (Table / CardGrid / DetailPage / Form / StatRow)",
      "description": "Pre-built skeleton variants matching standard page layouts. Drop in as <Suspense> fallbacks so the page silhouette stays in place while data loads.",
      "registryDependencies": ["skeleton"],
      "files": [
        {
          "path": "skeletons.tsx",
          "type": "registry:component",
          "target": "~/components/skeletons.tsx"
        }
      ]
    },
    {
      "name": "toast",
      "type": "registry:component",
      "title": "Toaster + toast (Sonner preset)",
      "description": "Pre-configured Sonner toaster with framework design tokens, plus a typed `toast` helper that extends Sonner with a `confirm` variant for low-stakes inline confirmations. Drop <Toaster /> once near root and call toast.success/error/promise from anywhere.",
      "dependencies": ["sonner"],
      "files": [
        {
          "path": "toast.tsx",
          "type": "registry:component",
          "target": "~/components/toast.tsx"
        }
      ]
    }
  ]
}
