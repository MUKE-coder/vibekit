{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "items": [
    {
      "name": "sparkline",
      "type": "registry:component",
      "title": "Sparkline",
      "description": "Tiny inline trend chart for stat cards / table cells / dashboard widgets. Line or area variant, optional tooltip, renders responsively into its parent. Built on Recharts.",
      "dependencies": ["recharts"],
      "files": [
        {
          "path": "sparkline.tsx",
          "type": "registry:component",
          "target": "~/components/sparkline.tsx"
        }
      ]
    },
    {
      "name": "metric-grid",
      "type": "registry:component",
      "title": "MetricGrid (KPI row with comparison)",
      "description": "Responsive grid of KPI cards with built-in trend arrow + colour, comparison-vs-previous-period rendering, optional sparkline slot, optional icon, and loading-skeleton state.",
      "dependencies": ["lucide-react"],
      "registryDependencies": ["card", "skeleton"],
      "files": [
        {
          "path": "metric-grid.tsx",
          "type": "registry:component",
          "target": "~/components/metric-grid.tsx"
        }
      ]
    },
    {
      "name": "date-range-picker",
      "type": "registry:component",
      "title": "DateRangePicker (dashboard presets)",
      "description": "Calendar-in-popover range picker tailored for dashboards. Bundles Today / Last 7 / Last 30 / Last 90 / This month / This quarter / YTD presets alongside a 2-month custom calendar.",
      "dependencies": ["lucide-react", "date-fns"],
      "registryDependencies": ["button", "calendar", "popover", "separator"],
      "files": [
        {
          "path": "date-range-picker.tsx",
          "type": "registry:component",
          "target": "~/components/date-range-picker.tsx"
        }
      ]
    },
    {
      "name": "donut-chart",
      "type": "registry:component",
      "title": "DonutChart",
      "description": "Donut/pie chart with built-in centre label slot, optional legend (with counts), and a theme-token-friendly default palette. Recharts-based.",
      "dependencies": ["recharts"],
      "files": [
        {
          "path": "donut-chart.tsx",
          "type": "registry:component",
          "target": "~/components/donut-chart.tsx"
        }
      ]
    },
    {
      "name": "funnel-chart",
      "type": "registry:component",
      "title": "FunnelChart",
      "description": "Conversion funnel chart with auto per-stage percentage labels and tooltip showing % of previous and % of top. Recharts-based.",
      "dependencies": ["recharts"],
      "files": [
        {
          "path": "funnel-chart.tsx",
          "type": "registry:component",
          "target": "~/components/funnel-chart.tsx"
        }
      ]
    },
    {
      "name": "heatmap-calendar",
      "type": "registry:component",
      "title": "HeatmapCalendar (GitHub-style activity grid)",
      "description": "Contribution-style heatmap with date-fns date math, configurable thresholds + cell size, per-cell tooltip, and an optional click handler. Use for activity feeds, deploys, commits, audit-log events, any 'did X happen today' signal.",
      "dependencies": ["date-fns"],
      "registryDependencies": ["tooltip"],
      "files": [
        {
          "path": "heatmap-calendar.tsx",
          "type": "registry:component",
          "target": "~/components/heatmap-calendar.tsx"
        }
      ]
    },
    {
      "name": "chart-card",
      "type": "registry:component",
      "title": "ChartCard (uniform dashboard widget shell)",
      "description": "Standard wrapper for every chart on a dashboard. Bundles title + description + optional actions dropdown + fullscreen toggle + loading skeleton + empty-state. Local `fullscreen` state expands the card to inset-4 while keeping slot DOM stable so chart libraries don't re-render from scratch.",
      "dependencies": ["lucide-react"],
      "registryDependencies": ["button", "card", "dropdown-menu", "skeleton"],
      "files": [
        {
          "path": "chart-card.tsx",
          "type": "registry:component",
          "target": "~/components/chart-card.tsx"
        }
      ]
    }
  ]
}
